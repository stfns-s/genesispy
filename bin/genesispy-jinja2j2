#!/usr/bin/env bash
# Local launcher for genesispy-jinja2j2 -- no install required; see _launch.
set -euo pipefail
exec "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/_launch" genesispy.tools.jinja2j2 "$@"

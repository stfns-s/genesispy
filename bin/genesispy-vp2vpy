#!/usr/bin/env bash
# Local launcher for genesispy-vp2vpy -- no install required; see _launch.
set -euo pipefail
exec "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/_launch" genesispy.tools.vp2vpy "$@"

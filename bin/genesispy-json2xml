#!/usr/bin/env bash
# Local launcher for genesispy-json2xml -- no install required; see _launch.
set -euo pipefail
exec "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/_launch" genesispy.tools.json2xml "$@"

#!/usr/bin/env bash
# Local launcher for genesispy-xml2json -- no install required; see _launch.
set -euo pipefail
exec "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/_launch" genesispy.tools.xml2json "$@"
